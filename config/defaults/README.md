# Default configuration

Shippable defaults seeded into the database on first run: tuning profiles
(scoring weights, format-mix rules), listing copy templates, mockup scene
prompt library, and pricing rules. These are generic wildlife/landscape-shop
defaults — your live tuning state lives in the database under `data/`, never
here.
